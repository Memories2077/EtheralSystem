FROM node:20-alpine

WORKDIR /app

COPY package*.json ./

RUN npm install -g npm@latest
RUN npm install

# Vá lại các lỗi/lỗ hổng package nếu có (bỏ qua nếu có breaking changes)
RUN npm audit fix || true

COPY . .

# Start the manager server
CMD ["npx", "tsx", "./src/mcp-server-manager.ts"]